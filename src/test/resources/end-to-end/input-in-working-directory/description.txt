Running the program on a file in the current directory would destroy that file, because the output
file is the input file.  The program recognizes that the input and the output are the same file,
writes a diagnostic, and exits with status 2 without touching the file.
