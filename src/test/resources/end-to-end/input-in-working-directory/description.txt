BUG: running the program on a file in the current directory destroys that file.  The program
deletes the output file before it reads the input file, and here they are the same file, so the
input is lost; the program then reads the now-empty file and exits successfully, without any
diagnostic.  The program should instead recognize that the input and output are the same file, and
refuse to process it.
