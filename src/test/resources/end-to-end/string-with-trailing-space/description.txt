A one-line @string definition with trailing whitespace after its closing brace is recognized as
complete, so it does not start a multi-line entry, and the text after it is removed.  The trailing
whitespace itself is retained, because the program copies lines verbatim.
