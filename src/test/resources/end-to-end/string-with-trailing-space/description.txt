BUG: the pattern for a one-line @string definition is anchored at the end of the line, so a
@string line with trailing whitespace is treated as the first line of a multi-line entry, and the
text after it is retained.  Trailing whitespace should be permitted.
