A line that consists of only whitespace and a closing brace -- such as the last line of a
multi-line field value, which is very common formatting -- does not end the entry, because it
closes a brace that the field value opened rather than the brace that the entry opened.  The whole
entry is retained, and the text after it is removed.
